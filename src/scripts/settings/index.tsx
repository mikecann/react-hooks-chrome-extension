import * as React from "react";
import * as ReactDOM from "react-dom";
import { ChildPageApp } from "../common/ChildPageApp";

console.log("hello from the settings page..");

ReactDOM.render(<ChildPageApp name="settings" />, document.getElementById("root"));
